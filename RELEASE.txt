Release process:
===============================================================================
 1) make sure logging entries are removed in Info.plist config configuration files;
 2) run 'ant dist' (or an individual plugin dist target);
 3) upload file (-s) to sourceforge;
 4) update Plex wiki pages if necessary:
   - http://wiki.plexapp.com/index.php/WikipediaRu
   - http://wiki.plexapp.com/index.php/KinoPoiskRu
   - http://wiki.plexapp.com/index.php/1TvRu
   - http://wiki.plexapp.com/index.php/WeeWzaTvRu
 5) make a release announcement.


KinoPoiskRu
===============================================================================

CURRENT TARGET
- Добавление опции выбора картинок ("брать все", "плохие не брать" и "не брать никаких");
- Улучшение определения выбора результатов:
  * использование оригинального названия фильма сравнения;
  * улучшение алгоритма сравнения года выпуска фильма;
- Упрощение конфигурации (удаление конфигурации логов);
- Устранение ошибок;
  * Устранение ошибки сортировки картинок;
  * устранение ошибки парсинга актеров (спасибо moddom);
  * устранение ошибки парсинга рэйтинга (спасибо moddom);
  * устранение ошибки выбора фильма в случае когда фильм найден сразу (спасибо moddom);

- Изменение конфигурации плагина больше не требует перезагрузки сервера;
- Добавление поддержки IMDB.

- Переход на использование логов Plex;

Добавлена опция выбора картинок. По умолчанию, "недоброкачественные" картинки будут
игнорироваться, что включает картинки с плохим разрешением и неподходящей ориентацией.
Также имеется возможность брать все картинки или не брать никаких.

Теперь по умолчанию, включена поддержка IMDB, что позволяет использовать плагин
KinoPoiskRu c другими плагинами требующими идентификатор IMDB, например   использующими информацию с


KinoPoiskRu-1.0
- Устранение ошибок выбора картинок (последствие изменений на сайте кинопоиска);
- Уменьшение количества запросов для выбора картинов (уменьшение трафика);
- Лицензирование кода лицензией GPL v2;

KinoPoiskRu-1.0rc3
- Добавление конфигурации логов;

KinoPoiskRu-1.0rc2
- Добавление конфигурации логов;

KinoPoiskRu-1.0rc1
- Улучшение определения правильности выбора фильма из результатов КиноПоиска;
- Добавление ролей и актеров со страницы Создателей фильма;
- Улучшение выбора обложек;
- Добавление возможности конфигурации плагина;
- Устранение различных ошибок:
  - неправильное определение продожительности фильма;
  - ошибок парсинга на Мак ОС X (на локале en_US);
- Улучшение работы с ошибками (error handling);
- Добавление логов для улучшения диагностики и устранения ошибок;


WikipediaRu
===============================================================================

CURRENT TARGET
- Переход на использование логов Plex;
- Упрощение конфигурации (удаление конфигурации логов);
-

WikipediaRu-0.92
- Добавление конфигурации логов;

WikipediaRu-0.91
- Поддержка большого количества тагов, в том числе актеры, роли, директор, студия, автор, жанр, и так далее;
- Использование имиджей с википедии, а также с Plex movieposterdb;
- Скачивание рэйтинга фильма с IMDB;
- Возможность создания Plex коллекций из категорий википедии.